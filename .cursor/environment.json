{
  "install": "pip install -e .",
  "start": "sudo service docker start",
  "terminals": [
    {
      "name": "interpreter-server",
      "command": "python -m interpreter --server --host 0.0.0.0 --port 8000",
      "description": "Runs the Open Interpreter LMC-compatible websocket server on port 8000"
    }
  ],
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  }
}